print("Hello Pycharm")
print(2**3 + (5+6) **(1+1))

#display: 129


""" Let's break down the expression step by step:
1. `(5 + 6)` evaluates to `11`.
2. `(1 + 1)` evaluates to `2`.
3. `11 ** 2` equals `121`.
4. `2 ** 3` equals `8`.
Now, let's substitute these values back into the original expression:

```
print(8 + 121)
```

Finally, when you add `8` and `121`, you get:

```
129
```

So, the result of the expression `print(2**3 + (5+6) **(1+1))` is `129` """